
[build]
  command = "npm run build"
  publish = "dist"

# Optional: single-page-app style fallback (not strictly needed here but safe)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
